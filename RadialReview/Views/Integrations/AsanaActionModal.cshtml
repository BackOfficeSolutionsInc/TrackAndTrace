@model RadialReview.Controllers.IntegrationsController.AsanaActionVM


@Html.HiddenFor(x => x.Id)
@Html.HiddenFor(x => x.SyncMyTodos)

<div>
	<div >Sync your to-dos up with an Asana workspace</div>
	<div class="form-group">
		@Html.LabelFor(x => x.Workspace)
		@Html.DropDownListFor(x => x.Workspace,(IEnumerable<SelectListItem>)ViewBag.Workspaces, new { @class = "form-control" })
	</div>
</div>